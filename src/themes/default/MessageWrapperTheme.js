import global from './Global';

const messageWrapper = {
  error: { color: global.errorTextColor, fontFamily: global.fontFamily },
  help: { color: global.helpTextColor, fontFamily: global.fontFamily },
};

export default messageWrapper;
